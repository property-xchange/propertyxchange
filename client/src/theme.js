import { extendTheme } from '@chakra-ui/react';

const theme = extendTheme({
  fonts: {
    body: 'myFont, system-ui, sans-serif',
    heading: 'myFont, system-ui, sans-serif',
  },
});

export default theme;
